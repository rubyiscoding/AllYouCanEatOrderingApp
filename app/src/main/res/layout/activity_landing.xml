<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Add a fancy TextView asking the user what they want to eat -->
    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="What would you like to eat today?"
        android:textStyle="bold"
        android:textSize="24sp"
        android:textColor="@android:color/black"
        android:layout_marginBottom="16dp" />

    <!-- Create text views like buttons for different categories -->
    <TextView
        android:id="@+id/categoryAppetizer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Appetizer"
        android:textSize="18sp"
        android:textColor="@android:color/black"
        android:clickable="true"
        android:padding="8dp"
        android:background="?android:attr/selectableItemBackground"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/categoryPopularItems"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Popular Items"
        android:textSize="18sp"
        android:textColor="@android:color/black"
        android:clickable="true"
        android:padding="8dp"
        android:background="?android:attr/selectableItemBackground"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/categoryBurgers"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Burgers"
        android:textSize="18sp"
        android:textColor="@android:color/black"
        android:clickable="true"
        android:padding="8dp"
        android:background="?android:attr/selectableItemBackground"
        android:layout_marginBottom="8dp" />

    <!-- Add other categories text views as needed -->

</LinearLayout>
